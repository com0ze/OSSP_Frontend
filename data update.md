# 데이터 로드 & 갱신 전략

## 1. 앱 시작 시 로드 (로그인 완료 직후)

| 데이터 | 이유 |
|---|---|
| 메인 유저 정보 | 모든 화면에서 사용 |
| 내 rentalItem 목록 | 마이페이지, 채팅 목록에 즉시 필요 |
| 내 match 목록 | 채팅 목록에 즉시 필요 |
| 내가 받은 리뷰 | 마이페이지에 즉시 필요 |
| match에 등장하는 상대방 유저 정보 | 채팅 목록, 마이페이지에서 이름 표시 |

---

## 2. 내 액션 후 즉시 갱신 (mutation trigger)

| 액션 | 갱신 대상 |
|---|---|
| 아이템 등록 | 내 rentalItem 목록 |
| 대여 요청 (매칭 생성) | match 목록, rentalItem |
| 매칭 수락 / 취소 | match 목록, rentalItem |
| 반납 완료 | match 목록, rentalItem, 메인 유저 정보 (rentalHistory) |
| 리뷰 작성 | 리뷰 목록, 상대방 유저 score |

---

## 3. 화면 진입 시 갱신 (on-demand, 매번 새로고침)

| 화면 | 갱신 대상 | 이유 |
|---|---|---|
| 상대방 프로필 | 해당 유저 정보 + 받은 리뷰 | 상대방이 변경했을 수 있음 |
| 채팅방 진입 | chat 메시지 목록 | 내가 없는 동안 메시지가 쌓임 |
| 홈 (rentalItem 목록) | 위치 기반 rentalItem | 새 글이 올라왔을 수 있음 |

---

## 4. 조건부 갱신 (특정 조건에서만)

| 조건 | 갱신 대상 |
|---|---|
| 위치 변경 | rentalItem 목록 |
| 앱이 백그라운드 → 포어그라운드 복귀 | match 목록 (상태 변경 놓쳤을 수 있음) |
| FCM 푸시 수신 | 푸시가 가리키는 타입만 invalidate |

---

## 5. 갱신하지 않아도 되는 것

- **reviewed 완료된 거래**: 상태가 더 이상 바뀌지 않음
- **과거 rentalHistory 아이템**: 읽기 전용, 변경 없음

---

## 참고: 실제 서버 전환 시 주의점

match 목록 로드 시 상대방 유저 ID만 추출 → `/users?ids=[...]` 로 배치 조회 필요.
개별 `getUserById`를 N번 호출하면 N+1 문제 발생.




- 리뷰 조회 요청이 없음
    - 가능성을 2개로 나눌 수 있음
    1. 유저를 조회할 때, 리뷰를 같이 불러온다.
    2. 다른 유저가 받은 리뷰를 볼 수 없다. 
        - 그럼 다음과 같은 문제가 생김 -> 메인 사용자의 리뷰는 어떻게 불러옴?

- 대여 취소 기능이 한가지로 구현됨
    - 대여자와 요청자의 취소가 구분된 프론트엔드와 다르게 백에서는 하나만 있음
    - 아마도 요청자의 취소가 구현된 것으로 보임